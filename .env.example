# Lakebase Postgres pooled connection string from the Neon Console
DATABASE_URL=

# Neon AI Gateway (branch host, not the database host)
NEON_AI_GATEWAY_BASE_URL=https://br-your-branch-api.ai.c-4.us-east-2.aws.neon.tech
NEON_AI_GATEWAY_TOKEN=nt_live_...

# Neon API key used by the benchmark runner to create model branches
NEON_API_KEY=

# Optional: protect benchmark runner endpoint
BENCHMARK_RUNNER_SECRET=change-me
